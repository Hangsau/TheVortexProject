# C 類主張蒐證 — codex 派工規格（第 2–4 批共用）

> 用途：Step 15／16 派 codex 蒐證時，本檔即完整指令，不需要再帶對話脈絡。
> 第 1 批（shoulder-arm，14 條）的成品是 `plans/證據包_C類_shoulder-arm.md`，格式以它為準。
> **裁決不派**：codex 只做蒐證並把「裁決」欄留空，裁決由主 session 做。

---

## 執行指令（放在派工 prompt 最前面，不可省略）

```
立即執行，不要輸出計畫，不要等我確認，不要問問題。
沒有寫出檔案就是任務失敗——exit 0 不算完成，只有目標檔存在且內容完整才算。
完成後回報：目標檔路徑、條目數、每條的第 1/2/3 層命中檔數。
```

## Git 操作禁令（違反即任務失敗）

```
禁止執行任何會改變 working tree 的 git 操作：
- git reset（任何形式，包含 --hard / --soft / --mixed）
- git pull --rebase / git rebase
- git stash / git stash pop
- git checkout -- <path> / git restore
- git clean（任何形式）
- git revert / git cherry-pick
如需「乾淨環境」，回報給呼叫方處理，不可自行清理。
本任務只允許新增／修改一個檔案（下方「本批參數」的目標檔），不得改動 canonical/ 任何檔案。
```

---

## 任務定義

C 類 = **游泳專屬的時序／推進主張**。這類主張的關鍵限制：**解剖教科書無法裁決它們**。
教科書只能否證「解剖上不可能」，不能證成「游泳時就是這樣動」。因此本任務只蒐證，
且必須把證據落在正確的層級上。

### 三層檢索，逐層都要留下實跑指令與逐字引文

| 層 | 路徑 | 說明 |
|---|---|---|
| 第 1 層 游泳書 | `C:\claudehome\resources\books\Science_of_Swimming_Faster\`（234 檔）<br>`C:\claudehome\resources\books\Fundamentals_of_Fast_Swimming\`（34 檔） | 教材層 |
| 第 2 層 本地文獻 | `C:\claudehome\resources\books\swimming-kinetic-chain\`（43 篇） | 已知覆蓋缺口：**蛙式、蝶式、踝、body roll 各 0 篇**。這些主題在本層查無時**不具推論力**，必須明寫「本層無覆蓋，非陰性結果」 |
| 第 3 層 線上 | Europe PMC REST、NCBI E-utilities | 先主題查詢篩選，再以 `resultType=core`、`fullTextXML` 或 PubMed `efetch` 取摘要／全文段落 |

### 讀法紀律（第 1 批踩過的坑，逐條都要套）

1. **引文與說明分開寫**。引文逐字，說明另起一行，不可混寫成一段。
2. **空間朝向不等於關節旋轉**。「掌心朝下」「拇指朝內」只記為空間朝向；
   除非研究實際量測前臂或肱骨，否則**不得**轉寫成旋前／旋後／內旋／外旋。
3. **教材的效益語句不等於研究終點**。教材說「這樣可以游更快」不構成有成績或
   效率終點的證據，記為教材主張，不記為文獻支持。
4. **頻率語言只能來自文獻**。發生率、盛行率、風險倍數、「好發」「常見於」——
   沒有文獻就是沒有，不得用負荷描述（「這個位置肩負荷大」）代替。
5. **不得自行下裁決**。「裁決」欄一律留空（`- 裁決：`），由主 session 填。

---

## 六個結構性根因（第 1 批歸納，本批先當篩子過一遍）

命中者優先處理，並在該條註明命中第幾號。根因 1 與 6 預期在其他分區同樣高頻。

| # | 根因 | 徵狀 |
|---|---|---|
| 1 | **效益句自由代換** | 原文把 A 動作的效益寫成 B 效益（划距／划頻／效率互換），教材原句其實講的是另一端 |
| 2 | **量化或比較主張缺對照／缺座標定義** | 給了角度或「比 X 大」，但沒說量的是什麼角、沒有同協議對照組 |
| 3 | **負荷描述冒充流行病學** | 用「負荷大」推出「好發」「易受傷」 |
| 4 | **由空間朝向反推關節旋轉** | 見讀法紀律 2 |
| 5 | **命名未溯源** | 相位名／路徑名只出自單一教學網站，無文獻出處 |
| 6 | **腕位／掌向憑解剖中立直覺填空** | 沒查就寫「腕伸直」；第 1 批 3 條全部寫反 |

## 硬規則：不得把「教材對撞」當結論

第 1 批唯一差點被錯殺的條目是 BK-29——教材的 `late rotation`（整體轉體來得太晚，
是錯誤徵狀）與主張的「推進段不轉」是**撞名而非矛盾**，逐句比對後教材其實支持主張。

因此：**蒐證階段不得寫出「教材與主張衝突」這種結論**。發現疑似衝突時，只做兩件事：
1. 把兩邊的原句**並列逐字列出**；
2. 分別寫出兩邊各自的**所指**（在講哪一段、哪個量、回答什麼問題）。

判斷是不是真衝突是裁決的工作，不是蒐證的工作。

---

## 輸出格式（每條一個區塊，欄位順序固定）

```markdown
## <ID>｜<主張標題>

- **原文主張（逐字）**：`...`；⚠︎ 註記（逐字）：`...`（若原清單有註記）
- **主張拆解**：① ... ② ... ③ ...
- **第 1 層（游泳書）檢索**：
  - 實跑指令：`rg -n -i -C 2 '...' '<path1>' '<path2>' -g '*.md'`
  - 命中檔：`...`
  - 原文句（逐字 + 檔名 + 行號）：
    - `檔名:行號`：
      > "..."
- **第 2 層（本地文獻）檢索**：（同上；本層無覆蓋時明寫「本層無覆蓋，非陰性結果」）
- **第 3 層（線上）檢索**：（同上，附查詢字串與 PMID／DOI）
- **綜合**：各層證據講到什麼程度，缺口在哪
- 裁決：
```

檔頭比照 `plans/證據包_C類_shoulder-arm.md`：生成日期、範圍、三層路徑與檔數、讀法。

---

## 本批參數

### Step 15 — 第 2 批 ankle-foot（3 條）

- 目標檔：`plans/證據包_C類_ankle-foot.md`
- 主張 ID：`FR-41`、`BR-35`、`ST-19`
- 原文出處：`plans/關節主張清單_自由式仰式.md`（FR-41）、`plans/關節主張清單_蛙式蝶式超流線.md`（BR-35、ST-19）
- 注意：**踝在第 2 層 0 篇**，三條都要靠第 1／3 層；本層查無一律標非陰性結果。

### Step 16a — 第 3 批 hip-knee（5 條）

- 目標檔：`plans/證據包_C類_hip-knee.md`
- 主張 ID：`FR-40`、`BR-29`、`BR-30`、`BF-32`、`BF-33`
- 原文出處：`plans/關節主張清單_自由式仰式.md`（FR-40）、`plans/關節主張清單_蛙式蝶式超流線.md`（其餘）
- 注意：蛙式、蝶式在第 2 層 0 篇。
- **`BR-30` 是特例，不套用一般三層檢索**：它已有附錄裁決
  （`plans/關節主張裁決_蛙式.md:426` 起，型 5 保留爭議），且相關的 `BR-23` 已判「修正」
  （同檔 `:315`）——這兩條**都不要重新蒐證**，先讀該檔對應段落。
  附錄裁決已把下一步定死：**不是選邊，而是查出爭議兩派的原始文獻各自用什麼測量方法**
  （體表標記反推髖旋轉 vs 分節段三維量測）。理由是若兩派測到的根本不是髖的旋轉
  （髖旋轉軸大部分落在股骨之外；膝屈曲時小腿可獨立旋轉），方法層對不上就不構成真正矛盾。
  因此 BR-30 這一條的蒐證產出只有一項：**兩派各自的原始文獻 + 每篇的測量方法逐字描述**。
  查不到測量方法描述時明寫「原文未報告測量方法」，不得以摘要措辭推測。

### Step 16b — 第 4 批 spine-neck／軀幹旋轉（12 條）

- 目標檔：`plans/證據包_C類_spine-neck.md`
- 主張 ID：`FR-10`、`FR-30`、`FR-31`、`FR-33`、`FR-43`、`FR-44`、`BR-40`、`BR-42`、`BR-43`、`BF-35`、`BF-36`、`ST-09`
- 原文出處：`plans/關節主張清單_自由式仰式.md`（FR-*）、`plans/關節主張清單_蛙式蝶式超流線.md`（其餘）
- 注意：**body roll 在第 2 層 0 篇**，軀幹旋轉相關條目查無時一律標非陰性結果。

---

## 派工指令範本

```bash
codex exec --sandbox danger-full-access --cd C:/claudehome/projects/TheVortexProject \
  "讀 plans/證據包_蒐證派工規格.md，執行其中「本批參數 → Step 15」一節。<貼上「執行指令」與「Git 操作禁令」兩個區塊>"
```

## 驗收（主 session 做，不看 exit code）

1. `git diff --stat` 確認**只有目標檔異動**，`canonical/` 零異動
2. 條目數與 ID 與本批參數完全一致
3. `grep -c "^- 裁決：$"` 等於條目數（裁決欄全空，codex 沒越界自行裁決）
4. 抽驗 3 條的逐字引文，開檔對行號確認引文真實存在
5. 檢查有無寫出「教材對撞／衝突」這類結論；有則退回
