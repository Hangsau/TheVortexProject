# (B) 組 13 相位：池畔可見方向／解剖學動作 兩欄核對

> 產出日期：2026-09-03。執行者：主 session（裁決工作，未派工）。
> 依據：`HANDOFF.md` 下一步建議第 0 項——「(B) 組 13 個相位走命名核對後才出 demand，兩欄不一致而未標明一律退件」。
> 素材：`canonical/instructional/technical-analysis.yaml` 的 starts-turns 48 筆（全部讀過 `title`；關節詞彙命中者讀 `public.summary` + `public.mechanism.text` 全文）。
> **本文件是核對表，不是 demand 草稿。** 核對過關的相位才進派工規格，未過關的相位在此說明卡在哪。

---

## 0. 先講三個推翻既有假設的結論

### 結論 1：(B) 的「素材已在庫內」不是齊頭成立的，要逐相位分開講

`HANDOFF.md` 第 197 行把 (B) 整組寫成「素材已在庫內，只差解剖命名核對」。**實測後這句話只對其中一半成立。**

48 筆 starts-turns 條目的 `category` 分布：

| category | 筆數 |
|---|---|
| `stroke-cycle` | 19 |
| `comparison` | 6 |
| `depth` | 4 |
| `streamline` / `errors` | 各 3 |
| `joint` | **2** |
| `hardware` / `concept` / `watersense` / `kick` | 各 2 |
| `head` / `coupling` / `tempo` | 各 1 |

只有 **2 筆**（`tech.47` 脊椎、`tech.48` 肩複合體）是 `category: joint`。用關節詞彙（髖／膝／踝／肩／肘／腕／脊椎／腰椎／胸椎／頸／骨盆／屈曲／伸展／外展／內收／旋前／旋後／內旋／外旋／蹠屈／背屈／肩胛／軀幹）掃過全部 48 筆的 `public` 全文，**27 筆命中 0 個詞**（逾半數）。

這 27 筆不是寫得不好，是它們本來就在回答別的問題——GRF 分工、最優深度、規則判定、量測指標一致性、器材效益。**它們描述「做什麼比較快」，不描述「哪個關節在動」。** 對這類素材做解剖命名核對，核對得出來的是空集合，不是內容。

**修正後的說法**：(B) 的正確描述是「**素材已在庫內，且其中一部分帶關節層內容**」。缺 ③（把動作長相對到解剖名）這個判斷仍然成立、仍然是工作量不是資料相依；但 ③ 的輸入在某些相位是空的，那些相位缺的就不只是 ③。逐相位結論見第 2 節。

### 結論 2：`flip` 與 `wall-rotation` 是整體剛體空間重定向，用 `joint-local` 命名是範疇錯誤

翻滾與牆上旋轉，池畔看到的是「身體轉了 180 度／90 度」。但這個轉動**不是任何一個關節做出來的**——它是整個身體繞著空間中一條軸旋轉，過程中各關節主要在**維持相對姿勢**（團身角度、流線手位）而不是在產生那個轉動。

這與其他相位有本質差異。`hip.flexion` 之類的動作名描述的是「遠端節段相對近端節段的相對運動」（`action_reference_frame: joint-local`）。「身體翻了一圈」在 joint-local 框架下**沒有對應動作**：翻滾時髖確實在屈曲（團身），但那是**團身**不是**翻滾**；把「翻滾」寫成 `hip.flexion` 會讓讀者以為屈髖等於翻滾。

**裁決**：這兩個相位若要進 movement 層，必須用 `action_reference_frame: poolside-fixed` 或 `body-fixed` 表達「身體在空間中的重定向」，而它們的 `action_ids` 只能列**維持姿勢與啟動旋轉的關節動作**，並在敘述中明說「旋轉本身不是關節動作」。這是結構決策，不是撰寫題——**因此 `flip` 與 `wall-rotation` 應從 (B) 移到 (C)**，理由與 C2/C3 同級（卡在裁決粒度，不卡在解剖）。

### 結論 3：核對抓到庫內兩處實質錯誤，兩處都要在寫 demand 前先修 `technical-analysis.yaml`

**錯誤甲 — `tech.42` 把「把肩往前推」命名為 hyperextension 與 sub-luxation。** 原文：

> Hyperstreamline 比一般 streamline 更激烈——**肩關節要做出 sub-luxation 或 hyperextension 把肩往前推**

兩個名字都錯，而且錯的方向相反：

- **hyperextension（過度伸展）** 在肩的命名裡是**手臂往身體後方去**。超流線要的是手臂在頭上、肩往前上方送，兩者方向相反。
- **sub-luxation（半脫位）** 是關節面部分失去對合的**病理狀態**，不是一個可以「做出來」的動作。把它寫成教學指令，等於叫泳者主動製造半脫位。

正解（且與 `tech.48` 同一套分層）：手臂上舉到底時把肩往前送，是**肩胛前突（scapular protraction）＋肩胛上迴旋＋肩複合體上舉**的合成，發生在肩胛胸廓那一層，不在盂肱關節。`tech.48` 已經裁決過「180 度是肩複合體合計角度、拆成盂肱約 120＋肩胛胸廓上迴旋約 60」；`tech.42` 這句話正是 `tech.48` 要防的那種「把整包掛在肩關節上」，只是它還多錯了一個方向。

**錯誤乙 — `tech.42` 的「腰椎反弓」與 `tech.47` 的裁決直接衝突。** `tech.47` 已裁決：「脊椎些微伸展」與「hollow body／避免拱腰」在同一軸上指向相反、同一份教材不能並存，必須擇一並寫明選哪個。`tech.42` 寫「胸腹腔縮窄、皮膚繃緊降低截面，**腰椎反弓**」——腰椎反弓即腰椎伸展，正是 `tech.47` 判為與 hollow body 互斥的那一邊。

這不是新的學派分歧，是 `tech.47` 已經裁決過的矛盾**在庫內還有一處沒清乾淨**。`tech.47` 的 `cross_ref_ids` 已經指向 `tech.42`，代表當時已看到關聯，但沒有回頭改 `tech.42` 的文字。

**兩者都不是「寫 demand 時順手繞過」就好**：demand 的 `derived_from_ids` 會指向這兩筆，讀者追過去就會讀到錯的命名。**先修 `technical-analysis.yaml`，再寫 demand。**

---

## 1. 核對過程中發現的缺登錄動作

`canonical/movement/actions.yaml` 目前 30 筆。(B) 組要用到但**尚未登錄**的：

| 需要的動作 | 誰要用 | 現況 | 判斷 |
|---|---|---|---|
| `neck.extension`（頸椎伸展） | `tech.4` 抬頭兩派、`tech.43` 起跳抬頭、`tech.20` 抬頭看牆破壞動量 | **完全沒有頸部動作** | 必須新增。`trunk.flexion/extension` 不能頂替——`tech.47` 全文都在區分頸椎／胸椎／腰椎三段，合成一個 `trunk` 就把該條的裁決抹掉了 |
| `neck.flexion`（頸椎屈曲） | `tech.42` 下巴貼胸、`tech.44` 下巴貼胸到 breakout 完成、`tech.20` 頭部朝下 | 同上 | 必須新增 |
| `shoulder-complex.scapular-protraction`（肩胛前突） | `tech.42` 超流線把肩往前送（即錯誤甲的正解） | 只有 `scapular-retraction`，缺對向 | 必須新增 |
| `ankle-foot.supination` | 暫無 (B) 條目需要 | 只有 `pronation`，缺對向 | **本輪不新增**。缺對向本身不是理由，沒有條目要用就不造（同 `gap.free.up-kick` 的處理原則） |
| `hip.internal-rotation` | 潛在用於背→蛙旋轉 | 只有 `external-rotation` | **本輪不新增**。該相位已移入 (C)，等結構決策後再看要不要 |

**新增三筆動作是 (B) 派工的前置**，且與 W6–W9 一樣要帶 `plane`／`axis`／解剖來源（`src.neumann-2017`）。頸部兩筆的 `joint_region` 用既有的 `spine-neck`。

---

## 2. 逐相位兩欄核對

欄位說明：**池畔可見方向** = 教練站在池邊或看影片會說出口的那句話；**解剖學動作** = 該現象在解剖命名下的正確名字；**一致？** = 兩欄是否指向同一個方向（不一致者是 demand 的核心內容）。

### 2.1 `starts-turns.set-position` 預備姿勢 — ✅ 可寫

素材：`tech.3`／`tech.4`／`tech.5`／`tech.9`／`tech.40`／`tech.41`

| 池畔可見方向 | 解剖學動作 | 一致？ |
|---|---|---|
| 「膝蓋彎到大概這個角度」（最優 90–120°，無跨選手通用值） | 膝屈曲（`knee.flexion`） | ✅ 一致 |
| 「臀部要高過肩膀」 | 髖屈曲（`hip.flexion`）＋軀幹前傾 | ✅ 一致 |
| 「頭要中立」／「頭朝前，不看水也不看牆」 | 頸椎中立（介於 `neck.flexion`／`neck.extension` 之間，**兩派爭的是起始位置不是動作**） | ⚠ **兩欄不對應**：這裡池畔講的是**姿勢**不是**動作**。demand 要寫的是「此相位頸椎位置有兩種教法且兩派都有菁英代表」，不是寫一個動作 |
| 「手抓台板往上拉」（後仰策略） | 肩複合體伸展（`shoulder-complex.extension`）＋肘屈曲或維持伸展（`elbow.flexion`／`elbow.extension`，`tech.41` 明說兩種效率接近） | ✅ 一致 |
| 「身體稍微往後 5–10°」（後仰策略） | 全身重心位移，**非關節動作**（各關節維持相對姿勢） | ⚠ 標明：這是重心策略不是關節動作 |

**裁決：可寫，claim_status 為 `disputed`。** 理由：`tech.3`／`tech.40` 明說膝角度無跨選手通用最優值、`tech.4` 明說頭位有兩派、`tech.41` 明說前傾後仰各有適用族群。**本層只列「這個相位牽涉哪些關節、有哪幾種並存的設置」，不裁決哪一種較好**（符合「列舉不裁決」的治理範圍）。

### 2.2 `starts-turns.takeoff` 起跳蹬離 — ✅ 可寫，且含一個 BK-30 級的兩欄不一致

素材：`tech.6`／`tech.7`／`tech.8`／`tech.16`／`tech.43`

| 池畔可見方向 | 解剖學動作 | 一致？ |
|---|---|---|
| **「後腿往上踢，踢到接近垂直、腳尖朝上」**（`tech.43`，能量貢獻最大的 coupling） | **髖伸展（`hip.extension`）＋膝伸展＋踝蹠屈** | ❌ **不一致，且是本組最重要的一筆**。池畔看到腿「往上」，直覺會對到「抬腿＝髖屈曲」；但起跳時軀幹前傾下潛，腿往上＝大腿往軀幹**後方**去＝**髖伸展**。與 BK-30（仰式上踢是髖屈曲）同一類錯誤，方向恰好相反 |
| 「大圓直臂繞上來」／「小圓彎肘」（`tech.43`，四種等級） | 肩複合體屈曲（矢狀面上舉，即 `shoulder-complex.elevation`）＋肘維持伸展或屈曲（`elbow.extension`／`elbow.flexion`） | ✅ 一致。**兩種繞臂差的是肘，不是肩**——大圓小圓走的是同一個肩動作，差在肘伸展與否改變了轉動慣量 |
| 「起跳瞬間抬頭看遠處」（`tech.43`／`tech.4`） | 頸椎伸展（`neck.extension`） | ✅ 一致 |
| 「雙腳蹬板」 | 髖伸展＋膝伸展＋踝蹠屈 | ✅ 一致 |
| **「臀部先出水再蹬腿」**（`tech.16`，仰式起跳） | 髖伸展先於膝伸展——**是兩個關節的時序，不是一個動作** | ⚠ 標明：池畔的一句話對到的是**關節間時序**。demand 要寫成「此相位髖伸展與膝伸展的先後有力學後果」，不能壓成單一動作 |

**裁決：可寫，claim_status 為 `partially-supported`。** 必寫內容：後腿踢的兩欄不一致（否則就是 `HANDOFF` 說的「兩欄不一致而未標明，一律退件」）。

### 2.3 `starts-turns.flight` 飛行 — ✅ 可寫（薄）

素材：`tech.10`／`tech.42`（前段）

| 池畔可見方向 | 解剖學動作 | 一致？ |
|---|---|---|
| 「在空中把姿勢整理好」（`tech.10` 明說飛行階段主要任務就是姿勢整理） | 上肢往流線收攏＋下肢伸展，**是姿勢達成不是產生推進** | ⚠ 標明：飛行期身體是拋體，**關節動作不改變重心軌跡**。這一點必須寫進 demand，否則讀者會以為飛行期的動作能改變落點 |
| 「腿不要打開」 | 髖內收方向維持中立（無 `hip.adduction` 登錄；現有只有 `hip.abduction`） | ⚠ 用「維持不外展」表達，不新造動作 |

**裁決：可寫，但內容薄且與 `entry` 高度重疊。** `tech.42` 的三段姿勢（超流線／髖屈 15°／腳尖伸直）文本本身是講**入水前**的姿勢構造，歸 `entry` 較準。**建議 flight 只寫「拋體期關節動作不改變重心軌跡、任務是姿勢達成」這一件事**，其餘讓給 `entry`，避免兩相位互抄。

### 2.4 `starts-turns.entry` 入水 — ✅ 可寫，內容最厚

素材：`tech.11`／`tech.12`／`tech.13`／`tech.42`

| 池畔可見方向 | 解剖學動作 | 一致？ |
|---|---|---|
| 「全身穿過同一個洞」（`tech.11`） | 各節段依序通過同一入水點——**是空間軌跡描述，不是關節動作** | ⚠ 標明。可對到的關節條件是「踝蹠屈維持」「髖不外展」 |
| 「腳尖完全伸直」（`tech.42`，最常見錯誤） | 踝蹠屈（`ankle-foot.plantarflexion`） | ✅ 一致 |
| 「髖屈大約 15°」（蛙式起跳放到 20–30°） | 髖屈曲（`hip.flexion`） | ✅ 一致 |
| 「下巴貼胸」（超流線） | 頸椎屈曲（`neck.flexion`） | ✅ 一致 |
| 「肘夾緊到幾乎觸碰、手腕重疊壓緊」 | 肩複合體水平內收（`shoulder-complex.horizontal-adduction`）＋肘伸展維持 | ✅ 一致 |
| **「把肩往前推」**（原文寫成 sub-luxation／hyperextension） | **肩胛前突（`scapular-protraction`）＋肩胛上迴旋＋肩複合體上舉** | ❌ **不一致，且原文命名錯誤**——見結論 3 錯誤甲。**修 `tech.42` 後才可寫** |
| 「輕微膝彎幫腳尖指得更垂直」 | 膝屈曲（`knee.flexion`） | ✅ 一致。註：與「大幅膝彎破壞流線」同欄，是程度差異不是方向差異 |
| 「腰椎反弓」 | 腰椎伸展（`trunk.extension`） | ❌ **與 `tech.47` 裁決衝突**——見結論 3 錯誤乙。**修 `tech.42` 後才可寫** |

**裁決：核對過關，但有兩處前置修正。** 修完 `tech.42` 才能派工。這一相位是 (B) 組內容最厚、兩欄不一致最多的一個。

### 2.5 `starts-turns.wall-contact` 觸牆 — ✅ 可寫

素材：`tech.21`／`tech.25`／`tech.22`（觸牆深度決定推蹬方向那部分）

| 池畔可見方向 | 解剖學動作 | 一致？ |
|---|---|---|
| 「觸牆時膝蓋彎 100–120°」（`tech.21`） | 膝屈曲（`knee.flexion`） | ✅ 一致 |
| 「腳觸牆深度 30–40 公分」（`tech.25`，蛙式／IM） | **空間位置，不是關節動作**；`tech.25` 另明說「髖部不應主動下沉來找牆」＝不主動髖屈曲 | ⚠ 標明。可寫的關節內容是那句否定式：**不以髖屈曲換取觸牆深度** |
| 「觸牆太深推蹬會偏上」 | 推蹬力向量方向，非關節動作 | ⚠ 標明為力學後果 |

**裁決：可寫，claim_status 為 `partially-supported`。** 註：`tech.21` 的 100–120° 與 `tech.3`／`tech.40`（set-position 膝角度無通用最優值）看似衝突，其實不是——一個講觸牆、一個講起跳台，且兩者都掛在同一條股四頭肌力–長度關係上。**demand 要把這兩個數字的適用相位分清楚，不得互相引用。**

### 2.6 `starts-turns.wall-push-off` 蹬牆推離 — ✅ 可寫

素材：`tech.18`／`tech.22`／`tech.47`／`tech.48`

| 池畔可見方向 | 解剖學動作 | 一致？ |
|---|---|---|
| 「腳離牆前流線就要做好」（`tech.18`） | 上肢流線姿勢在蹬伸前達成——**時序要求** | ⚠ 標明為關節間時序 |
| 「蹬直」 | 髖伸展＋膝伸展＋踝蹠屈 | ✅ 一致 |
| **「手臂舉到 170–180 度」**（`tech.48`） | **肩複合體合計角度，非盂肱單一關節**（教學近似：盂肱約 120＋肩胛胸廓上迴旋約 60，且該近似取自很寬的估計範圍） | ❌ **不一致，且 `tech.48` 已寫好正解**。這是庫內唯一一筆已經自己做完兩欄核對的條目，直接引用即可 |
| 「腰要平／不要拱腰」（hollow body） | **`tech.47` 裁決：與「脊椎些微伸展」互斥，必須擇一並寫明** | ❌ **不一致且庫內有裁決**。demand 必須沿用 `tech.47` 的裁決，不得自行擇一後當成共識寫 |

**裁決：可寫，且是 (B) 組品質最高的一個**——兩筆 `category: joint` 素材都落在這裡，兩欄核對在庫內已經做完，寫 demand 等於把既有裁決搬進 movement 層。

### 2.7 `starts-turns.turn-approach` 進牆 — ⚠ 只能寫一筆，內容極薄

素材：`tech.19`／`tech.20`／`tech.23`／`tech.38`

| 池畔可見方向 | 解剖學動作 | 一致？ |
|---|---|---|
| 「不要抬頭看牆」（`tech.20`） | 頸椎伸展（`neck.extension`）——**此相位要的是不做這個動作** | ✅ 命名一致；內容是「維持不伸展」 |
| 「最後一划要划完整」（`tech.19`） | 屬 free 的划手相位，不是本相位的關節內容 | ⚠ 不納入 |
| 「靠計步判距」（`tech.19`／`tech.23`） | 神經控制策略，**零關節內容** | ❌ 不可寫成關節 demand |
| 「翻滾啟動時機」（`tech.38`） | 時序，且指向 `flip`（已移 (C)） | ⚠ 不納入 |

**裁決：可寫但只有一筆，內容是「此相位的關節要求是頸椎維持不伸展」。** 這相位的素材主體（計步、時機）**在本層根本不可表達**——它們是感知與策略內容，屬 Technica／Bridge 層，不屬 movement。**寫 demand 時必須明說本層只覆蓋到這麼多**，不得為了讓條目看起來飽滿而把計步策略改寫成關節語言。

### 2.8 `starts-turns.breaststroke-pullout` 蛙式水下划臂 — ❌ 不可寫，缺關節層素材

素材：`tech.26`／`tech.27`／`tech.35`

| 池畔可見方向 | 解剖學動作 | 一致？ |
|---|---|---|
| 「三種 Pullout 順序（Combined／Fly-Kick First／Pull-Down First）」 | **順序差異，`tech.26` 明說三者無統計顯著差異** | ❌ 零關節內容 |
| 「水下滑行深度 0.4–0.6 m」 | 空間位置 | ❌ 零關節內容 |
| 「蛙式不用 UDK，改 pullout」 | 規則與技術選擇 | ❌ 零關節內容 |

**裁決：不可寫。** 三筆素材全部是技術選擇比較、深度最優值與規則說明，用關節詞彙掃過 `public` 全文命中 0。**這一個相位缺的不是 ③（核對），是 ①（動作長什麼樣子的關節層描述）本身在庫內就沒有。**

修法（不是 blocker，是工作）：pullout 的划臂軌跡在解剖上可由現有 `breast` 素材推導（蛙式划手已有 BR 系列裁決），但那是**跨相位借用**，屬 (C3) 同類決策。**本輪不寫，移入 (C)，理由「素材缺關節層」而非「卡結構」——這是 (C) 之外的第三類，見第 3 節。**

### 2.9 `starts-turns.flip` 翻滾 — ❌ 移入 (C)

見結論 2。素材 `tech.20`／`tech.24`／`tech.38` 全部在講時機、規則與旋轉動量，翻滾本身是剛體空間旋轉，`joint-local` 框架無對應動作。**卡在 `action_reference_frame` 的結構決策，不卡在解剖命名。**

### 2.10 `starts-turns.wall-rotation` 牆上旋轉 — ❌ 移入 (C)

見結論 2。素材 `tech.29`／`tech.30`／`tech.31`／`tech.37` 全部在講 FINA 規則的兩個時間點（觸牆時仰臥、離牆時俯臥）、三種轉法的推出速度比較與常見犯規。**「肩部旋轉 ≥90°」的「肩部」在原文是身體方位的代稱，不是肩關節動作**——這正是兩欄核對要抓的混用，但抓到之後發現正解是「整體軸向重定向」，同樣落在 `joint-local` 表達不了的範疇。

### 2.11 `starts-turns.breakout` 出水 — ✅ 可寫

素材：`tech.44`（七要點）

| 池畔可見方向 | 解剖學動作 | 一致？ |
|---|---|---|
| 「下巴貼胸到 breakout 完成才抬頭」（要點⑥） | 頸椎屈曲維持（`neck.flexion`），延後 `neck.extension` | ✅ 一致 |
| 「第一划手肘在身體內側、不外掃」（要點④） | 肘屈曲（`elbow.flexion`）＋肩複合體避免水平外展（`horizontal-abduction`） | ✅ 一致 |
| 「前伸手繼續往前送、指緊掌平、肘不彎」（要點⑤） | 肩複合體上舉維持＋肘伸展（`elbow.extension`）＋腕中立（不 `wrist-hand.flexion`／`extension`） | ✅ 一致 |
| 「維持 hyperstreamline 不提前分手」（要點③） | 肩複合體水平內收維持 | ✅ 一致 |
| 「第一划與最後一下海豚下踢同步」（要點①） | **跨相位時序**，不是關節動作 | ⚠ 標明 |
| 「第一划不換氣」（要點⑦） | 呼吸決策，零關節內容 | ❌ 不納入 |

**裁決：可寫。** 這相位的七要點裡有四點可對到關節動作，且**四點全部是「維持某姿勢／延後某動作」而非「產生某動作」**——這個共同結構本身就是該相位最值得寫的內容。

### 2.12 `udk.breakout` UDK 出水 — ✅ 可寫，但與 2.11 是同一批素材

`HANDOFF` 把 `udk.breakout` 的素材指向 `tech.44`，而 `tech.44` 的 `stroke` 欄實際是 **`starts-turns` 不是 `udk`**（全庫沒有 `udk.tech.44`）。

**裁決：可寫，但必須與 `starts-turns.breakout` 分清楚誰擁有什麼。** 建議：`udk.breakout` 寫「最後一踢與第一划的銜接」（`tech.34`／`tech.44` 要點①②），`starts-turns.breakout` 寫「出水期的流線維持與抬頭延後」（要點③④⑤⑥）。**兩者不得互抄，也不得用 `cross_ref` 假裝已經分開。**

### 2.13 `fly.entry-pause` 蝶式入水停頓 — ⚠ 需先確認素材真的存在

`HANDOFF` 第 200 行寫「BF-01～04 的入水段敘述已含停頓描述，只差把它與 `fly.entry` 分開命名」。**本次核對未驗證這句話**——BF 系列的裁決在 `plans/關節主張裁決_蝶式.md`（本輪未讀）。

**裁決：暫掛。** 派工前必須先做一件事：讀 BF-01～04 原文，確認「停頓」在素材裡是**一個被描述的相位**（有起訖、有姿勢內容），還是只是敘述中的一個修飾詞。若是後者，這一相位就與 2.8 同類——缺的是素材本身，應移入 (C4)（同 `fly.breathing-window`）。**不得因為 `HANDOFF` 寫了「已含」就直接派工。**

---

## 3. 核對後的重新分組

| 原分組 | 相位 | 核對結果 | 新分組 |
|---|---|---|---|
| (B) | `set-position` | ✅ 過關（disputed） | **(B) 可派工** |
| (B) | `takeoff` | ✅ 過關，含 BK-30 級兩欄不一致 | **(B) 可派工** |
| (B) | `flight` | ✅ 過關但薄，須與 entry 切乾淨 | **(B) 可派工** |
| (B) | `entry` | ✅ 過關，**前置：先修 `tech.42` 兩處** | **(B) 可派工（修完後）** |
| (B) | `wall-contact` | ✅ 過關 | **(B) 可派工** |
| (B) | `wall-push-off` | ✅ 過關，品質最高 | **(B) 可派工** |
| (B) | `turn-approach` | ⚠ 過關但只有一筆內容 | **(B) 可派工（須明說覆蓋邊界）** |
| (B) | `breakout` | ✅ 過關 | **(B) 可派工** |
| (B) | `udk.breakout` | ✅ 過關，須與上一列切乾淨 | **(B) 可派工** |
| (B) | `breaststroke-pullout` | ❌ 庫內零關節層素材 | **→ (D) 新類：素材缺關節層** |
| (B) | `flip` | ❌ 剛體旋轉，`joint-local` 表達不了 | **→ (C) 結構決策** |
| (B) | `wall-rotation` | ❌ 同上 | **→ (C) 結構決策** |
| (B) | `fly.entry-pause` | ⚠ 素材存在性未驗證 | **暫掛，先讀 BF-01～04** |

**(D) 是本次核對新開的一類**：素材在庫內、相位有登錄、也不卡結構決策，但庫內那幾筆素材**回答的是別的問題**（技術選擇、最優深度、規則），沒有任何關節層描述可供核對。(D) 與 (C) 的差別是：(C) 缺的是裁決，(D) 缺的是內容；(D) 的解法是去寫關節層素材（或從鄰近 stroke 借用並標明），不是等決策。

**數字結算**：(B) 從 13 個縮到 **9 個可派工** + 1 個暫掛 + 2 個移 (C) + 1 個移 (D)。

---

## 4. 派工前的前置工作清單（按順序）

1. **修 `canonical/instructional/technical-analysis.yaml` 的 `tech.42`**：① 把「肩關節要做出 sub-luxation 或 hyperextension 把肩往前推」改成肩胛前突＋上迴旋＋肩複合體上舉的分層寫法（引 `tech.48`）；② 把「腰椎反弓」依 `tech.47` 的裁決處理——`tech.47` 要求擇一並寫明選哪個，本處直接沿用該裁決的結論框架，不自行另擇。**這是主 session 的裁決工作，不派工。**
2. **在 `canonical/movement/actions.yaml` 新增三筆**：`neck.extension`／`neck.flexion`／`shoulder-complex.scapular-protraction`，帶 `plane`／`axis`／`src.neumann-2017`。可派工（verbatim spec）。
3. **讀 `plans/關節主張裁決_蝶式.md` 的 BF-01～04**，決定 `fly.entry-pause` 進 (B) 還是 (C4)。
4. **依 W6–W9 的八段式格式寫派工規格**，9 個相位分 2–3 批（建議：起跳段 `set-position`／`takeoff`／`flight`／`entry` 一批；牆段 `wall-contact`／`wall-push-off`／`turn-approach` 一批；出水段 `breakout`／`udk.breakout` 一批）。
5. **每份規格的負面清單必含**：不得裁決哪種設置策略較好、不得把計步／時機／規則改寫成關節語言充數、不得把 `flip`／`wall-rotation`／`breaststroke-pullout` 順手補上、**不得在兩欄不一致處只寫解剖名而不寫池畔說法**（讀者要能從自己聽過的那句話找到這條記錄）。

---

## 5. 本文件抓到而 `HANDOFF` 需更正的三句話

- **「(B) 13 個素材已在庫內，只差解剖命名核對」** → 9 個成立，`breaststroke-pullout` 不成立（缺關節層素材），`flip`／`wall-rotation` 不成立（缺結構決策），`fly.entry-pause` 未驗證。
- **「這 11 個相位每一個都有對應條目」的逐相位對映漏了 `tech.47`／`tech.48`** → 而這兩筆是全庫 starts-turns 唯二的 `category: joint`，且 `tech.48` 是庫內唯一一筆已經自己做完兩欄核對的條目。對映應補：`wall-push-off ← tech.6/18/22/47/48`。
- **「breakout ← tech.44」寫成 `udk.tech.44`** → 實際 id 是 `starts-turns.tech.44`，全庫沒有 `udk.tech.44`。
