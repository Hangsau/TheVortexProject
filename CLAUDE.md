# The Vortex Project — 工作指引

> 狀態、待辦、進度見 `HANDOFF.md`。本文件是規則與背景知識，不常改動。

---

## 專案本質

以**水感知為核心**的游泳教學研究系統，個人使用，兼作教學工具。

**核心命題：所有游泳技術都建立在水感知上。**
技術形式是感知系統的輸出，不是輸入。直接教動作形式會跳過感知建立——疲勞一來，動作退回感知真正穩定的地方。

作者：Hangsau（有游泳教學實務經驗）
性質：個人研究 + 純粹好奇，不設發表目標

**教學對象：涵蓋全光譜，沒有單一主客群** — 兒童、兒童競技、青少年競技、鐵人三項、成人初學；未來將擴充自由潛水（成人 + 兒童）。
→ 撰寫任何內容（尤其心理 / 動機 / 恐懼 / 感知層）**不得預設或窄化到單一族群**；族群特異差異要分列並列，不可把任何一群當「主要對象」。AI（含子代理）不得自行推斷「主客群」——文獻偏壓在某族群（如兒童泳課、競技隊）是**素材缺口**，不是受眾界定。

---

## 外部資源

| 資源 | 連結 |
|------|------|
| 水感教學研究記錄（Google Sheets） | https://docs.google.com/spreadsheets/d/1s4XFWtXz0PzrsGYyaRdeN17kkpbYVtvDx5JYGicWIls/edit?gid=0#gid=0 |

本地原始檔：`水感教學研究記錄.xlsx`（根目錄）

---

## 系統架構

```
Instructional/   → 物理層：四式技術的物理條件與生物力學
Technica/        → 感知層：L0–L6 水感框架
Bridge/          → 橋接層：感知目標語言
Research/        → 研究層：文獻筆記與前沿假設
Observations/    → 觀察層：教學案例
```

完整文件清單見 `_INDEX.md`

---

## 關鍵概念（必須理解才能有效協作）

**L0–L6 水感框架**
感知發展的序列，不是技術難度的序列。L0 是呼吸感知建立，L6 是全身場感知。
每個 L 級有對應的訓練方法、外部觀察指標、三型診斷。

**三型診斷（A/B/C型）**
從 L3 開始適用。診斷感知缺陷的主要方向：
- A型：手感缺失（EVF 崩潰、只靠手掌划水）
- B型：腳感缺失（踢水節律失守）
- C型：全身張力問題（換氣或轉體觸發感知遮蔽）

**感知目標語言**
每個技術點需要有三層描述：
① 物理描述（現有技術分析已有）
② 感知目標（泳者應該感覺到什麼）
③ 感知失敗的樣子（感覺錯了是什麼感覺）

**硬體邊界 vs 感知缺陷**
框架中最重要的前置判斷：學生做不到某個動作，是感知沒建立，還是解剖限制（踝關節/髖內旋/肩活動度）？兩者介入方向完全不同，混淆是最常見的教學錯誤。

**確定性層次標記**
推導🔵 / 近期文獻🟢 / 舊文獻🟡 / 教練觀測🟠 / 個人假設🔴

---

## 注意事項（給 AI）

- 討論技術時，優先從「感知目標」角度切入，而非動作形式
- 「硬體邊界」問題不要用感知訓練介入，兩者混淆是框架最容易出錯的地方
- 區分確定性層次，在適當位置標注上方色圓
- **診斷框架中的指標必須是感知描述，不能是動作形式描述**：EVF、手過中線、S 形路徑等都是感知系統的輸出，不是診斷目標。提案 A/B/C 型定義或訓練指標時，問自己「這在描述感知狀態，還是動作形式？」——後者要轉換成前者才能進框架。
- **動手前查證**：遇到可疑引用（年份 2026+、跨式引用、數字不一致）先 WebSearch 確認，查完再動筆。不要先寫再說「我沒驗證」
- **WebSearch 摘要裡的數字不等於可信數字**：物理數值、生理數據必須追蹤到原始來源（論文、教科書）才算查證完成；標 🟡 前確認是公認物理常數，不能只憑搜尋摘要。
- **人名歸因是高風險幻覺區**（如 USRPT 作者可能被捏造為虛構名字）：不確定來源時標 🔴 或省略，不要寫入。
- **刪除主體內容時，同步更新所有摘要表格**（感知層連結摘要、快速查核表等），立即刪除對應行，不留到最後才清理。

## 誤區深探收錄標準

每條誤區必須通過這個篩選：

> **「教練主動這樣教」還是「選手自然犯錯、教練沒有糾正」？**

- ✅ 收錄：教練主動說出口的口令或指導觀念（即使是錯的）
- ❌ 不收錄：選手因恐懼/慣性自然出現的動作，教練只是沒糾正

例：「抬頭看牆確認距離」——教練不會主動這樣教，是初學者的自然反應。不屬於誤區深探範疇。

**提案清單時**：每條若不是行業內廣泛流傳的口令，必須附一句說明「哪種教練/教材這樣教」，讓用戶能判斷是否成立。

## 🔴 查證工作流

遇到待查（🔴）項目時，固定流程：

1. **並行搜尋**：一次送出 3 條 WebSearch（不同切角），再針對最有料的結果深挖 1–2 輪
2. **三段決策**：
   - 找到可信來源 → 填入內容，標 🟢（或 🟡/🟠，依來源性質）
   - 找到部分線索但無法完全確認 → 補充說明/解釋假說，保留 🔴
   - 確認是研究空白 → 明確寫「目前文獻無此數據」，保留 🔴，不要留白
3. **不接受「搜不到」作為結束**：先確認是真的研究空白，還是搜尋關鍵字不對——換關鍵字再試一輪
4. **🔴 表格完整性**：每次更新「剩餘 🔴（N 處）」表格後，必須數一遍實際行數確認 N 正確——標題數字與行數不符是常見錯誤。同時確認文中所有 🔴 標記都已在表格中有對應條目。

---

## AI 收尾規則（強制）

每次對話結束前，若本次做了任何實質修改（新增文件、更新內容、格式整理等），**必須**：
1. **凡動到 canonical/ 或 Drills/ 的內容**：先跑 `python tools/build_knowledge_map.py` **與 `python tools/build_indices.py`**，重生 `KNOWLEDGE_MAP.md` 與 `indices/*.json`，再 commit。兩者都是 committed 產物、都不能落後於實際內容——地圖是人讀入口，`indices/` 是機器索引（內容／tag 反向／來源反向／缺口報告）。**`indices/` 先前只寫在 S5 完成紀錄裡沒進本流程，結果整整 24 個動到 canonical 的 commit 沒重生（record_count 停在 839，實際 930）**
2. 更新 `HANDOFF.md` 的「當前狀態」與「下一步建議」兩個區塊
3. 更新 `_INDEX.md` 的對應文件狀態（如有變動）
4. 若 Research/ 層有新文件完成：同步 `RESEARCH_PLAN.md`（Layer 完成標記）+ `FUTURE_RESEARCH.md`（原題目畢業標記）
5. `git add` + `git commit` + `git push` 到 GitHub。**本 repo 的線上分支是 `master`，不是 `main`**——Claude Code 的 session context 會自稱「Main branch: main」，那是預設值不是實況；用 `git push origin HEAD:main` 會憑空建出一條沒人看的分支，且 `notify-mysite` 只監聽 `master`，等於推了卻不同步
6. **此 repo 雖無 Hugo build CI，但 push 後 notify-mysite workflow 會通知 my-site 自動同步**——確認 `.github/workflows/notify-mysite.yml` 觸發後才算完整流程

不需要等使用者提醒。這是固定流程。

**「下一步建議」更新標準**：完成大任務後整段重寫，不只改最後一句——舊內容可能已過期。

## 加新內容前的標準流程

凡是要對 canonical/ 加新條目或修改既有條目，**必須**：
1. 先讀 `KNOWLEDGE_MAP.md` 對應章節，確認沒有近似條目（避免重複）
2. 若已有近似條目 → 修現有條目，不另開
3. 若有不確定的範圍，用 ID 去 `grep canonical/` 看實際內容再決定
4. 動完之後重跑地圖

這是為了避免「一下說缺一下說沒缺」的反覆——地圖是真相，不靠記憶。

## 散文欄位的 YAML 純量寫法

- **內容自己有行結構（列點、需要保留的換行）→ 必須用 `|-`。** `>-` 會把換行折成空白，**結構在下游拿到字串之前就已經不存在**，那是資料層的破壞：它不報錯，只讓網站看起來像渲染 bug。
- **內容本來就是一整段、只是在檔案裡折行好讀 → `>-` 可以留。** 折點產生的句中空白由 my-site 的 `sync_vortex.py`（`dump_yaml()`）接掉，canonical 不必為此改寫、也不要為此改寫。
- **刪掉 `text:` 這種多行鍵時，續行要一起刪。** 只刪鍵和第一行，剩下的續行會被下一個鍵吸收，同時造成「原欄位消失」與「新欄位尾巴多一截不屬於它的句子」——2026-05 的 `beece5a` 一次弄壞 20 條 `physical_reason`，撐到 2026-09 才被肉眼發現。**改完多行欄位後 grep 一次該欄位是否還在。**

## 知識索引與證據規範（canonical）

Vortex 已有 `KNOWLEDGE_MAP.md`、穩定 ID、`cross_ref`、確定性與公開／診斷分層，這些是基礎；新增或重構資料時必須把它們升級為可驗證資料契約，而非只靠 YAML 內的自由文字。

- **canonical 是唯一真相來源**：標籤、來源、交叉參照與內容只在 `canonical/` 維護；`my-site/data/vortex/` 是同步產物，不得手改補標籤或來源。
- **受控詞彙表**：新增可跨條目使用的分類、泳式、L 級、受眾、風險、內容類型或概念標籤前，先登錄於 canonical 的 taxonomy；條目只能引用既有 key。需要同義字時寫 alias，不另造近義 tag。
- **來源註冊表**：任何 🟢／🟡 的外部事實、數字、療效或安全主張，都要以 `source_id` 指向可重取的來源登錄（DOI／PMID／ISBN／正式 URL、作者、年份、來源類型、查驗日）。文字 citation 只能是閱讀顯示，不是資料鍵。**反過來也成立：機器鍵不得出現在讀者看得到的散文裡**——`caveat`／`prevalence`／`description` 這些欄位會原樣上線，寫 `src.akkurt-2017` 讀者只會看到一串 slug，要寫「Akkurt et al. 2017」；schema 欄位名（`fatal_acute: true`）與內部檔案路徑同理。稽核方式：線上頁面 grep 反引號，有反引號幾乎就是機器鍵外洩。
- **可驗證關係**：`cross_ref` 必須指向存在的穩定 ID；條目新增、刪除或改 ID 後，重生地圖前先跑引用完整性、tag 合法性、來源存在性與孤兒條目檢查。
- **索引輸出**：由 canonical 自動產生四個視圖：內容／概念索引、tag 反向索引、來源反向索引、缺口報告（無來源高風險主張、孤兒標籤、未連結條目）。`KNOWLEDGE_MAP.md` 是人讀入口，不取代這些機器索引。
- **狀態分開**：`draft`、`indexed`、`source-linked`、`verified`、`disputed`／`unverified` 不可互相冒用；確定性標記也不能代替來源驗證狀態。

### 證據分層契約（S3b，驗證器強制）

Vortex 不套單一文獻門檻——那會讓感知層永遠不合格。改成**兩種證據等級並存，各自有自證義務**；欄位語意登錄在 `canonical/_taxonomy.yaml#evidence_contract`，執行者是 `tools/validate.py`，兩邊必須同時改。

- **標 certainty 前先判斷證據性質，不預設標綠**：物理／解剖推導是 🔵（不需來源），教學實務觀察是 🟠（不需來源但要 `observation_basis`），只有真有文獻才標 🟢／🟡（必須有來源）。判準逐值寫在 taxonomy `certainty.values[].criterion`。W009 的正解通常是改標記，不是補一個空引用。
- **文獻證據**：`source_ids`（可解析成 `_sources.yaml` 的 `src.<slug>`）是唯一機器鍵；`source`／`sources`／`citation` 三個顯示字串只給人讀，算「已有來源」但仍是待遷移狀態（W002 追蹤）。
- **實務證據**：🟠 條目寫 `observation_basis`，內含誰觀察／什麼族群／外推邊界三項。它**不冒充文獻**——不得寫「研究顯示」；也不需要 `source_ids`。缺此欄位 → W011。
  **「教練觀測」不得登錄成來源**（E014）：它是觀察本身，不是可回溯的東西。把它寫進 `_sources.yaml` 會兩頭出事——讀者看到一個長得像引用的東西、點進去只有四個字；W011 的來源逃生口被它滿足，於是「🟠 要交代觀察基礎」被一個內容就是「教練觀測」的登錄擋掉（循環自證）。正解是在引用它的區塊寫 `observation_basis`。判準是**整串相等**：「教練觀測 2024–2025」算（扣掉年份只剩觀察行為），「Aaron Peirsol／Ryan Murphy 水下影像分析（教練觀測）」不算（指名了對象與媒材）。
- **墓碑不得被引用**（E015）：`retracted` 仍留在 `_sources.yaml`，所以它**仍在 E005 的合法集合裡**——引用一筆已判定不可引用的來源本來會完全靜默。撤下某來源時要同時處理它現有的引用，不能只改 `verification_status`。
- **來源繼承**：祖先區塊帶來源時子區塊不重複要求，但繼承只在同一條目（最近的 `id`）子樹內成立，不跨兄弟條目。
- **綜述行**：結論的證據由子條目承擔時寫 `evidence_from: [子條目 ID]`；空 list `[]` 不算宣告。
- **公開／診斷分層是結構性的**：診斷判讀語（`perception_probe`／`signal_structure`／`discriminators`／`type_diagnosis`／`manipulation`／`contrast_question`…）只能放 `diagnostic` 子樹。`my-site/tools/sync_vortex.py` 對 `public` 整包 `rec.update(pub)`（白名單），所以新增 diagnostic 型兄弟鍵不會外洩，**唯一實際洩漏路徑是把診斷內容寫進 `public` 底下**——這條是 ERROR 級（E010），不是警告。
